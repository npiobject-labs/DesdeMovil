# Sesión 20260925-2244

1. **Qué cambió:** tres peticiones sobre Peripatéticos, tras la entrega de las 20:15. **Google Drive dentro del recorrido**: paso 6, opcional; el usuario crea a mano una carpeta con el nombre de la app y pega su enlace, y el instalador lo anota en `CLAUDE.md` del hijo. **Claude comprueba que llega a Drive**: paso 8; ni la página ni el PC pueden, así que Claude sigue la nueva sección «Comprobación de Drive» de `CLAUDE.md`, deja una ficha en la carpeta y escribe `docs/drive.json`, que la página vigila. **Copia de seguridad en el PC**: paso 7/7 del instalador, en `Documentos\Peripateticos\<app>\` con el repositorio entero (zip, sin git), ficha, accesos directos y `Actualizar copia.cmd`. La portada explica ahora dónde queda todo, Drive incluido; el recorrido pasa a 19 pantallas.
2. **SHA:** `12bac53` en `main` (PR [#22](https://github.com/npiobject-labs/DesdeMovil/pull/22), squash). Pages [36187525275](https://github.com/npiobject-labs/DesdeMovil/actions/runs/36187525275) **success**, sin run de Jekyll; sin despliegue en Fly (no toca `app/`). Pruebas: 64 comprobaciones de extremo a extremo en Chromium, `Actualizar copia.cmd` ejecutado de verdad y los escenarios del instalador. Destaparon dos fallos, arreglados antes de publicar: ids de Drive falsos de 15 caracteres dados por buenos, y el aviso de Drive no válido que no salía porque en PowerShell `$script:Drive` y el parámetro `$Drive` son la misma variable.
3. **URLs para probar:** https://npiobject-labs.github.io/DesdeMovil/ (build `DM-B3-20260925-007`; «Dónde queda todo» y pasos 6 y 8) · https://npiobject-labs.github.io/DesdeMovil/recorrido.html (build `DM-B3-20260925-009`; pantallas 4, 15 y 16) · https://npiobject-labs.github.io/DesdeMovil/pc.html?app=prueba&org=TU-ORG
4. **Drive:** copia de este resumen en `sesiones/` y del análisis actualizado como `peripateticos-analisis-v2.md` en la carpeta configurada en **Parámetros**.
5. **Qué falta:** la primera ejecución real en Windows con cuentas de prueba, ahora incluyendo la carpeta de Drive y el paso 8 con una sesión real de Claude en el repositorio hijo. Dos supuestos nuevos, con plan B en la sección 12 del análisis: que el conector de Drive escribe en una carpeta creada a mano, y que `MyDocuments` resuelve bien con OneDrive.
