# Sesión 20260909-2103

1. **Qué cambió:** verificado el estado tras fusionarse el PR #7 y cambiada la fila **Carpeta de Drive (id)** de la tabla **Parámetros** de `CLAUDE.md` a `10437HObdpr75FWMAhUlGzbJf9JKURHIT` (carpeta `inversion` de *Mi unidad*, con escritura). La inicialización **no aplica a este repo**: es la plantilla, conserva `.plantilla-pendiente` y `init-plantilla.yml` sale `skipped` por la barrera `is_template == false`, que es su comportamiento correcto aquí.
2. **SHA:** `847526b` en `claude/determined-newton-m6v7lm`, PR [#8](https://github.com/npiobject-labs/DesdeMovil/pull/8) en borrador, rama rearrancada desde `main` porque el #7 ya estaba fusionado. En `main`, el merge `b9b272a` con `pages.yml` run [34391998935](https://github.com/npiobject-labs/DesdeMovil/actions/runs/34391998935) en **success** y sin ningún run `pages build and deployment` detrás: el Source de Pages sigue en «GitHub Actions».
3. **URLs para probar:** https://npiobject-labs.github.io/DesdeMovil/ (mock, build `DM-B3-20260905-001`) · https://npiobject-labs.github.io/DesdeMovil/guiaNew.html (`DM-B3-20260909-008`) · https://npiobject-labs.github.io/DesdeMovil/guiaFableMini.html (`-009`) · https://npiobject-labs.github.io/DesdeMovil/guiaFable.html (`-010`).
4. **Drive:** copia de `docs/planificacion/` completa en la carpeta nueva, con los resúmenes en `sesiones/`.
5. **Qué falta:** fusionar el PR #8 para que la id de Drive entre en `main`. Sigue pendiente de decidir lo de la sesión anterior: con `FLY_API_TOKEN` heredado, `deploy.yml` despliega ya en el commit inicial de cada repo nuevo, así que Fly deja de ser opcional de hecho; y la app derivada de este repo pasa a `desdemovil-npiobject-labs`, con `desdemovil-npiobject` huérfana en Fly.
