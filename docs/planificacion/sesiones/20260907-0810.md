# Sesión 2026-09-07 08:10 — el residuo que rompía la inicialización

1. **Qué cambió:** revisión de si el proyecto está terminado. Encontrado un bloqueante real introducido por `637805f`: `docs/guiaNew.html` traía `guia-desdemovil-v2` como clave de `localStorage`, el sed de `init-plantilla.yml` solo sustituye `DesdeMovil` y `npiobject` en `docs/*.html`, y el paso antirresiduos hace `grep -i desdemovil` — así que **cualquier repo creado hoy desde la plantilla habría fallado el run**. Invisible aquí porque en la plantilla el workflow sale `skipped`. Arreglado el residuo y endurecido el workflow: sustituye también la minúscula, y aparta/restaura el enlace a la plantilla de origen (`github.com/npiobject/DesdeMovil` y el `template_name`/`template_owner`) en `docs/*.html`, no solo en `ARRANQUE.md` —hasta ahora las guías heredadas quedaban en el hijo apuntando a un repo inexistente—, con el `grep` excluyendo las dos formas. De paso, fuera los enlaces a `PruebaPlantilla` (desechable) y corregida la frase de `guiaNew.html` que daba los `tools/*.ps1` por probados: no lo están. Cerrados los PR [#2](https://github.com/npiobject/DesdeMovil/pull/2) y [#3](https://github.com/npiobject/DesdeMovil/pull/3), superados por el portado a mano de `63c590a`.
2. **SHA:** `3785340` en la rama `claude/proyecto-finalizado-epoxsh`, no en `main`: la sesión venía con la rama impuesta por el operador. Queda pendiente el merge para que `pages.yml` publique.
3. **URLs para probar:** https://npiobject.github.io/DesdeMovil/guiaNew.html tras el merge; el mock sigue en https://npiobject.github.io/DesdeMovil/ (`DM-B3-20260905-001`). La verificación de fondo no es Pages: es el paso antirresiduos, simulado sobre un árbol limpio con `pepe/CasaVerde`, `otro/proyecto_2` y `npi/DM2`, verde en los tres.
4. **Drive:** copia de este resumen en `sesiones/` de `Mi unidad/DesdeMovil` (id `1-0wWhp_-rrSgxKrr0AN34dg_Y2nAPK2J`), sin borrar nada.
5. **Qué falta:** decidir cuál de las cuatro guías (`guia`, `guiaFable`, `guiaNew`, `guiaFableMini`) es la oficial y borrar las demás; `guiaNew` y `guiaFableMini` siguen sin `<meta name="build">` y sin enlace desde `README.md`/`ARRANQUE.md`. Y el de siempre: **[SUPUESTO]** los `tools/*.ps1` nunca se han ejecutado; plan B, pasar `-Root` explícito. `PruebaInit` es desechable cuando quieras.
