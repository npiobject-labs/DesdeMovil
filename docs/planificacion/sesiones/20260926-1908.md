# Sesión 20260926-1908

1. **Qué cambió:** en la primera ejecución real en Windows, al abrir `montar-<app>.cmd`, salió la ventana gris «Advertencia de seguridad de Abrir archivo — No se pudo comprobar el editor» (Ejecutar / Cancelar) en vez de la azul de SmartScreen que describía la guía. `pc.html`, la portada y la pantalla 7 del recorrido explican ahora las dos ventanas. Además, el `.cmd` se quita a sí mismo la marca de «descargado de internet» (`Unblock-File`), así que al reabrirlo para seguir donde lo dejó Windows ya no avisa.
2. **SHA:** `a5c4dc0` en `main` (PR [#26](https://github.com/npiobject-labs/DesdeMovil/pull/26), squash). Pages [36257933270](https://github.com/npiobject-labs/DesdeMovil/actions/runs/36257933270) **success**, sin run de Jekyll. Probado en Chromium: páginas sin errores de JS y `.cmd` generado solo en ASCII.
3. **URLs para probar:** https://npiobject-labs.github.io/DesdeMovil/pc.html?app=recetas&org=apps-de-maria (build `DM-B3-20260926-001`) · https://npiobject-labs.github.io/DesdeMovil/ (build `DM-B3-20260926-002`) · https://npiobject-labs.github.io/DesdeMovil/recorrido.html#7 (build `DM-B3-20260926-003`)
4. **Drive:** copia de este resumen en `sesiones/` de la carpeta configurada en **Parámetros**.
5. **Qué falta:** terminar la ejecución real en Windows que está en curso. [SUPUESTO] `Unblock-File` quita la marca aunque `cmd.exe` esté leyendo el archivo; plan B: si no la quitara, al reabrir saldría el mismo aviso y bastaría pulsar «Ejecutar» otra vez, como ya explica la guía.
